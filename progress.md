Original prompt: create a KOF-style Moroccan-themed production-ready game, first search the web for repos that would help; use imagegen for characters and animations inspired by the pet workflow.

## 2026-05-12

- GoalBuddy board created at `docs/goals/kof-moroccan-game`.
- T001 repo research completed. Recommendation: greenfield TypeScript combat core with Phaser shell.
- T002 workspace scout completed. Repo was empty except GoalBuddy docs.
- T003 asset pipeline scout completed. `OPENAI_API_KEY` is missing, so image generation is blocked for now.
- T004 Judge selected the first implementation slice: scaffold Vite/TypeScript + Phaser shell + tested deterministic combat core.
- T005 implementation created the Vite/TypeScript scaffold, pure combat core, Phaser debug shell, core tests, and browser verification hooks.
- Verification passed: `npm install`, `npm run typecheck`, `npm test`, `npm run build`, and web-game Playwright run against `http://127.0.0.1:5173/`.
- Browser screenshot issue was fixed by using the Canvas renderer for this debug shell instead of WebGL capture.
- T007 added game-native asset manifests for fighters and the first Moroccan stage, with validation tests keeping future imagegen work tied to required animation rows, source status, and anti-trademark constraints.
- T008 added match-flow rules for timer outcomes, round-over snapshots, and HUD status display.
- T010 added a tested shell flow reducer and Phaser UI states for ready, fighting, and round-over, with start/reset/restart controls and control help.
- T011 added a best-of-three match-set layer with score pips, round labels, match-over shell flow, and tests for round/set progression.
- T012 added deterministic P2 CPU sparring input, a CPU/manual toggle, visible mode state, and controller tests.
- T013 audited the playable tranche and confirmed the full outcome remains incomplete until generated art/animations are produced and integrated.
- T014 added per-animation source status plus a FighterState-to-manifest animation map with procedural fallback renderer metadata.
- T015 added deterministic imagegen job specs for canonical fighter references, animation rows, and stage layers, all blocked until `OPENAI_API_KEY` is available.
- T016 added deterministic hit/block visual effects with a pure lifecycle helper, renderer drawing, browser text-state effect counts, and tests.
- T017 added an asset runtime resolver that promotes only approved generated rows with output paths and fails closed to procedural fallback for blocked, planned, generated-but-unapproved, or pathless assets.
- T018 audited the post-runtime artifact and confirmed the full goal is still incomplete until imagegen assets exist.
- T019 imagegen execution preflight is blocked because `OPENAI_API_KEY` is still missing.
- T020 wired runtime sprite/fallback descriptors into the live browser state for the current fighter animations.
- T021 added P/Escape pause/resume shell behavior that freezes simulation until resumed.
- T022 added deterministic combo tracking to combat snapshots and HUD/browser output.
- T023 added deterministic easy/normal/hard CPU profiles for pressure range, reactions, and attack cadence.
- T024 exposed CPU difficulty cycling in the playable shell and browser state.
- T025 audited the playable foundation and confirmed generated imagegen content remains the full-goal blocker.
- T026 added stage runtime layer descriptors with procedural fallback and browser-visible stage metadata.
- T027 added a fighter-select shell state backed by the two current Moroccan fighter definitions.
- T028 audited character select and confirmed generated imagegen content remains the full-goal blocker.
- T029 added asset-readiness summaries for imagegen job counts, credential blockers, runtime fallbacks, and next required assets.
- T030 added a visible select-screen readiness overlay for blocked imagegen jobs, fallback counts, and next required assets.
- T031 polished select readiness text wrapping and combo HUD readability.
- T032 audited the polished artifact and identified README/runbook as the final safe local slice before imagegen credentials are needed.
- T033 added README run/test/control documentation and the imagegen unblock path.
- T034 imagegen execution remains blocked because `OPENAI_API_KEY` is still missing.
- T035 credential handoff remains blocked because `OPENAI_API_KEY` is still missing.
- T036 added `npm run verify` as the one-command local quality gate.
- T037 added a GitHub Actions CI workflow that runs `npm ci` and `npm run verify`.
- T038 imagegen production execution remains blocked because `OPENAI_API_KEY` is missing.
- T039 added `npm run imagegen:preflight` for local credential readiness checks.
- T040 imagegen generation remains blocked because preflight still reports `OPENAI_API_KEY=missing`.
- T041 added an imagegen runbook with job counts, first jobs, output conventions, and QA checks.
- T042 local reproducible imagegen execution remains blocked because `OPENAI_API_KEY` is missing, but the Codex app imagegen session produced a usable Moroccan dual-fighter concept sheet.
- T043 copied that concept sheet into `assets/source/imagegen/fighters/`, marked both fighter canonical references as generated-but-unapproved, and kept animation rows/stage layers blocked until production imagegen jobs are completed.
- T044 exposed the generated concept sheet in the live character-select screen as cropped Atlas Lion and Sahara Viper previews, hid fight HUD/debug bodies on selection, and verified select/fight browser states.
- T045 generated Atlas Lion and Sahara Viper idle row candidates through the Codex app imagegen path, saved them as generated-but-unapproved source art, and verified the runtime still falls back procedurally until QA approval.
- T046 added `npm run imagegen:qa`, a Node-only generated-row QA report that checks PNG dimensions against expected frame counts and 256x256 runtime cells without failing normal verification while rows are intentionally unapproved.
- T047 added `scripts/normalize-idle-rows.mjs` and exact-size idle QA artifacts under `output/imagegen/*-idle-normalized.png`; QA now distinguishes raw generated source rows from normalized runtime-cell candidates.
- T048 audited the normalized idle rows and rejected them for runtime approval because the PNGs have no alpha channel and retain visible checker-style backgrounds, even though their dimensions are correct.
- T049 replaced the checker-background idle candidates with one-fighter Atlas Lion and Sahara Viper chroma-key green candidates, regenerated normalized 2048x256 QA artifacts, and kept them unapproved pending a fresh approval audit.
- T050 deferred runtime approval for the chroma-key rows because whole-row normalization makes fighters look vertically compressed; the next slice should fit/key each frame cell separately.
- T051 updated idle-row normalization to fit each frame into its own 256x256 cell and key out green backgrounds into alpha; normalized idle artifacts are now 2048x256 RGBA PNGs while manifest rows remain unapproved.
- T052 composited the alpha-keyed idle rows over magenta for visual QA and rejected runtime approval because Atlas Lion has per-cell split artifacts/body-part clipping, even though Sahara Viper is closer.
- T053 regenerated Atlas Lion idle with wider gutters, replaced the Atlas candidate, reran per-cell alpha normalization, and verified magenta composites no longer show severe Atlas split artifacts.
- T054 integrated the normalized Atlas Lion and Sahara Viper idle rows as approved runtime sprites, copied them into `public/assets/generated/fighters/`, updated readiness counts, and verified browser/runtime state while preserving procedural fallback for non-idle states.
- T055 audited the idle integration and selected `walk-forward` rows for both fighters as the next content-production slice, using Codex app imagegen auth for generation and local scripts for normalization/QA while keeping shell `OPENAI_API_KEY` preflight as a reproducibility blocker.
- T056 generated a two-row Atlas Lion/Sahara Viper `walk-forward` candidate sheet through the Codex app imagegen path, split it into per-fighter source rows, generalized normalization/QA beyond idle, produced 2048x256 RGBA normalized candidates, and marked walk-forward rows generated-but-unapproved in the manifest.
- T057 approved the normalized `walk-forward` candidates for runtime integration after alpha/dimension checks and magenta-composite visual QA.
- T058 copied the approved `walk-forward` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in the Phaser scene, added a local `?demo=walk-forward` verification path, and captured browser proof at `output/web-game/walk-forward-runtime.png`.
- T059 audited the two-animation runtime artifact and selected `light-punch` rows for both fighters as the next content-production slice because it most improves combat readability with a bounded six-frame scope.
- T060 generated a two-row `light-punch` candidate sheet through the Codex app imagegen path, split it into per-fighter source rows, fixed normalization atomic writes and frame-slice detection, produced 1536x256 RGBA normalized candidates, and marked the rows generated-but-unapproved in the manifest.
- T061 approved the normalized `light-punch` candidates for runtime integration after alpha/dimension checks and magenta-composite visual QA.
- T062 copied the approved `light-punch` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=light-punch`, and captured browser proof at `output/web-game/light-punch-runtime.png`.
- T063 audited the three-animation runtime artifact and selected `hitstun` rows for both fighters as the next fight-readability slice so generated attacks gain generated reaction art.
- T064 generated a two-row `hitstun` candidate sheet through the Codex app imagegen path, split it into per-fighter source rows, produced 1280x256 RGBA normalized candidates, and marked the rows generated-but-unapproved in the manifest.
- T065 approved the normalized `hitstun` candidates for runtime integration after alpha/dimension checks and magenta-composite visual QA.
- T066 copied the approved `hitstun` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=hitstun`, and captured browser proof at `output/web-game/hitstun-runtime.png`.
- T067 audited the four-animation runtime artifact and selected `blockstun` rows as the next bounded content slice because defense readability is now the highest-leverage gap after generated hit reactions.
- T068 generated a two-row `blockstun` candidate sheet through Codex app imagegen, split it into per-fighter source rows, normalized both to 1280x256 RGBA runtime candidates, and marked `blockstun` generated-but-unapproved in the manifest.
- T069 approved the normalized `blockstun` candidates for runtime integration after alpha/dimension checks and magenta-composite visual QA.
- T070 copied the approved `blockstun` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=blockstun`, and captured browser proof at `output/web-game/blockstun-runtime.png`.
- T071 audited the five-animation runtime artifact and selected `heavy-punch` as the next content slice because the K-key heavy attack is already playable but still procedural.
- T072 generated, rejected, and repaired heavy-punch candidates; the repaired Atlas Lion and Sahara Viper rows are normalized 2048x256 RGBA candidates and marked generated-but-unapproved pending approval audit.
- T073 approved the repaired normalized `heavy-punch` candidates for runtime integration after alpha/dimension checks and magenta-composite visual QA.
- T074 copied the approved `heavy-punch` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=heavy-punch`, and captured browser proof at `output/web-game/heavy-punch-runtime.png`.
- T075 audited the six-animation runtime artifact and selected `special` as the next content slice because the L/Enter special command is already playable but still procedural.
- T076 generated and rejected a crowded `special` candidate, regenerated a compact repaired candidate, normalized both fighter rows to 2560x256 RGBA, and marked `special` generated-but-unapproved pending approval audit.
- T077 approved the repaired normalized `special` candidates for runtime integration after alpha/dimension checks and magenta-composite visual QA.
- T078 copied the approved `special` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=special`, and captured browser proof at `output/web-game/special-runtime.png`.
- T079 audited the seven-animation runtime artifact and selected Marrakesh Rooftop stage layer candidates as the next content slice because the Moroccan stage presentation still relied entirely on procedural fallback.
- T080 generated four Marrakesh Rooftop stage layer candidates through Codex app imagegen, rejected a first fake-transparent foreground candidate, regenerated it with chroma green, scaled all accepted layers to 1024x576, marked the stage layers generated-but-unapproved, and verified browser state still keeps stage runtime on procedural fallback.
- T081 rejected direct stage runtime approval because the first far/playfield candidates were not true stackable layers and foreground still needed chroma-key normalization.
- T082 repaired far-architecture and playfield with cleaner imagegen candidates, keyed the chroma layers to alpha, created 1024x576 RGBA runtime-candidate layers plus `output/imagegen/stages/marrakesh-rooftop/runtime-candidates/composite-preview.png`, and kept the manifest/live runtime generated-but-unapproved.
- T083 approved the normalized Marrakesh Rooftop runtime-candidate stage pack for Phaser integration.
- T084 copied the approved stage layers into `public/assets/generated/stages/marrakesh-rooftop/`, marked all four stage layers approved, rendered them in Phaser behind fighters with procedural fallback preserved, and captured browser proof at `output/web-game/stage-runtime.png`.
- T085 audited the generated stage runtime artifact and selected `knockdown` as the next fighter row because it is already a real KO/round-ending combat state.
- T086 generated an initial knockdown candidate, rejected it for overlapping poses, regenerated a cleaner separated sheet, split/normalized Atlas Lion and Sahara Viper knockdown rows to 2048x256 RGBA candidates, and marked `knockdown` generated-but-unapproved.
- T087 approved the repaired normalized `knockdown` rows for runtime integration after visual QA.
- T088 copied the approved `knockdown` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=knockdown`, and captured browser proof at `output/web-game/knockdown-runtime.png`.
- T089 audited the knockdown runtime artifact and selected `walk-back` as the next fighter row because it is an existing defensive movement state and complements approved `walk-forward`.
- T090 generated a clean separated `walk-back` sheet, split/normalized Atlas Lion and Sahara Viper walk-back rows to 2048x256 RGBA candidates, and marked `walk-back` generated-but-unapproved.
- T091 approved the normalized `walk-back` rows for runtime integration after visual QA.
- T092 copied the approved `walk-back` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=walk-back`, and captured browser proof at `output/web-game/walk-back-runtime.png`.
- T093 audited the walk-back runtime artifact and selected `crouch` as the next bounded fighter row because it is already a real defensive/low state in the combat map.
- T094 generated a clean Codex app imagegen `crouch` sheet, split/cleaned Atlas Lion and Sahara Viper source rows, normalized both to 1024x256 RGBA candidates, and marked `crouch` generated-but-unapproved. Verification passed: `node scripts/normalize-fighter-rows.mjs --animation crouch`, `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, and `npm run build`.
- T095 rejected crouch runtime approval because magenta-composite QA revealed opaque black cell backgrounds in the normalized rows.
- T096 repaired the fighter-row normalizer to preserve input transparency and tightened chroma-key detection so Atlas teal costume pixels are not removed. Recreated crouch source crops from the original Codex app imagegen sheet, regenerated transparent 1024x256 crouch candidates, added a regression test for opaque-black padding, and verified with `npm run imagegen:qa`, `npm run typecheck`, `npm test`, and `npm run build`.
- T097 approved the repaired normalized `crouch` rows for runtime integration after magenta-composite visual QA confirmed transparent backgrounds, intact Atlas teal costume, and readable low guarded silhouettes.
- T098 copied the approved `crouch` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=crouch`, and captured browser proof at `output/web-game/crouch-runtime.png`. Verification passed: public sprite `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, `npm run build`, and browser state with `runtimeVisuals.p1.assetKey=atlas-lion:crouch`.
- T099 audited the crouch runtime artifact and selected `jump` as the next bounded fighter row because it is already a real core movement state, while `light-kick`, `win`, and `lose` are still not active runtime states.
- T100 generated a clean Codex app imagegen `jump` sheet, split Atlas Lion and Sahara Viper source rows, normalized both to 1536x256 RGBA candidates, and marked `jump` generated-but-unapproved. Verification passed after fixing a stale readiness expectation: `node scripts/normalize-fighter-rows.mjs --animation jump`, `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, and `npm run build`.
- T101 approved the normalized `jump` rows for runtime integration after magenta-composite QA confirmed transparent backgrounds and readable takeoff/apex/landing poses.
- T102 copied the approved `jump` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, added `?demo=jump`, and captured browser proof at `output/web-game/jump-runtime.png`. Verification passed: public sprite `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, `npm run build`, and browser state with `runtimeVisuals.p1.assetKey=atlas-lion:jump`.
- T103 audited the jump runtime artifact and selected a playable `light-kick` command/state slice before light-kick imagegen work, because light-kick is still manifest-only and not an active core command.
- T104 added a playable `lightKick` command/state with `I` key input, tested hit behavior, animation mapping to `light-kick`, procedural fallback rendering, and `?demo=light-kick` browser proof at `output/web-game/light-kick-procedural.png`.
- T105 generated a clean Codex app imagegen `light-kick` sheet, split Atlas Lion and Sahara Viper source rows, normalized both to 2048x256 RGBA candidates, and marked `light-kick` generated-but-unapproved. Verification passed after increasing the intentionally heavy asset QA test timeout: `node scripts/normalize-fighter-rows.mjs --animation light-kick`, `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, and `npm run build`.
- T106 approved the normalized `light-kick` rows for runtime integration after magenta-composite QA confirmed transparent backgrounds and readable startup/extension/recovery poses.
- T107 copied the approved `light-kick` rows into public runtime assets, marked them approved in the manifest, preloaded/rendered them in Phaser, tuned `?demo=light-kick` to show the extension frame, and captured browser proof at `output/web-game/light-kick-runtime.png`. Verification passed: public sprite `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, `npm run build`, and browser state with `runtimeVisuals.p1.assetKey=atlas-lion:light-kick`.
- T108 audited the light-kick runtime artifact and selected `win` as the next bounded match-presentation row because match-over currently has labels but no generated victory pose row.
- T109 generated a clean Codex app imagegen `win` sheet, split Atlas Lion and Sahara Viper source rows, normalized both to 2048x256 RGBA candidates, and marked `win` generated-but-unapproved. Verification passed: `node scripts/normalize-fighter-rows.mjs --animation win`, normalized-row `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, and `npm run build`.
- T110 approved the normalized `win` candidates after alpha/dimension checks and magenta-composite QA confirmed readable respectful victory poses without visible green/black fill, labels, weapons, or severe clipping.
- T111 copied the approved `win` rows into public runtime assets, marked them approved in the manifest, added match-over presentation resolution for winner `win` sprites, added `?demo=win`, and captured browser proof at `output/web-game/win-runtime.png`. Verification passed: public sprite `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, `npm run build`, and browser state with `runtimeVisuals.p1.assetKey=atlas-lion:win`.
- T112 audited the win runtime artifact and selected `lose` generation as the next bounded task because lose rows are now the only remaining fighter animation fallback.
- T113 generated a clean Codex app imagegen `lose` sheet, split Atlas Lion and Sahara Viper source rows, normalized both to 1536x256 RGBA candidates, and marked `lose` generated-but-unapproved. Verification passed: `node scripts/normalize-fighter-rows.mjs --animation lose`, normalized-row `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, and `npm run build`.
- T114 approved the normalized `lose` candidates after alpha/dimension checks and magenta-composite QA confirmed readable respectful defeated poses without visible green/black fill, labels, weapons, gore, or severe clipping.
- T115 copied the approved `lose` rows into public runtime assets, marked them approved in the manifest, rendered match-over losers with `lose` sprites while winners use `win`, and captured browser proof at `output/web-game/lose-runtime.png`. Verification passed: public sprite `sips`, `npm run imagegen:qa`, `npm run typecheck`, `npm test`, `npm run build`, and browser state with zero blocked imagegen jobs and zero runtime fallbacks.
- T116 final audit rejected immediate completion because `README.md` and `docs/imagegen-runbook.md` still described the older credential-blocked/idle-only state even though runtime assets are now complete.
- T117 refreshed `README.md` and `docs/imagegen-runbook.md` to describe the current generated-art-complete runtime state, Codex app imagegen provenance, QA commands, and CLI key note. Verification passed: stale-doc `rg` guard, `npm run imagegen:qa`, and `npm run verify`.
- T118 final audit passed and marked the GoalBuddy goal complete. Completion evidence: source-backed repo research, pet-inspired imagegen workflow, playable TypeScript/Phaser combat shell, generated Moroccan fighter/stage runtime assets, zero blocked imagegen jobs, zero runtime fallbacks, updated docs, `npm run imagegen:qa`, `npm run verify`, and browser proof at `output/web-game/lose-runtime.png`.
- Post-goal animation polish: fixed glitchy runtime animation by adding deterministic sprite-frame plans so non-looping attacks/reactions/presentation rows clamp or hold instead of modulo-wrapping, improved idle row normalization so Atlas/Sahara idle sprites use actual per-frame character bounds, regenerated all normalized/public fighter sheets with steadier bottom alignment, and captured browser proof at `output/web-game/animation-polish-live-after.png`, `output/web-game/animation-polish-heavy-after.png`, and `output/web-game/animation-polish-win-after.png`. Verification passed: `npm run imagegen:qa`, `npm test`, `npm run build`, and `npm run verify`.
- Second animation polish pass: replaced hold-only combat poses with curated one-shot timelines for attacks/reactions and intentional loops for idle/walk/win, moved whiff/action streaks and hit/block sparks onto a high-depth effect overlay, aligned contact effects with the painted sprite bodies, added render-only close-range sprite separation to reduce silhouette clipping, cache-busted runtime sheets with `timeline-polish-1`, and captured browser proof at `output/web-game/timeline-polish-live.png`, `output/web-game/timeline-polish-heavy.png`, `output/web-game/timeline-polish-special.png`, and `output/web-game/timeline-polish-win.png`. Verification passed: `npm run imagegen:qa` and `npm run verify`.
- Paid-quality presentation tranche: rebranded the playable shell as `ATLAS ARENA`, rendered generated Marrakesh rooftop art behind title/select, replaced concept-sheet select crops with approved transparent runtime idle sprites, added tested presentation/readiness helpers, added camera impact shake plus a subtle custom flash wash, added procedural WebAudio cues/music for UI/hit/block/round/match events, added an `F` fullscreen toggle, and split the production build into a small game chunk plus isolated Phaser vendor chunk. Verification passed: `npm run imagegen:qa`, `npm run verify`, and web-game Playwright screenshots/state at `output/pro-final-title`, `output/pro-final-select`, and `output/pro-final-combat`.
- Follow-up UI/assets correction after owner rejection: removed player-facing asset-pipeline copy, rebuilt the title/select surfaces around transparent runtime fighter art, added stronger title showcase frames, VS selection cards, calmer top ornament, HUD fighter nameplates, and verified the title/select/combat screenshots at `output/rework-title`, `output/rework-select`, and `output/rework-combat`. Verification passed: `npm run verify`.
- KOF engine correction after owner rejected the core feel: added power meter/runtime snapshots, standalone special input, QCF super command routing, normal-to-special/super cancel windows, run/backdash/directional-hop/roll states, move-specific stun frames, meter gain on use/hit/block, super spend logic, HUD power bars, and regression tests for the new mechanics. Verification passed: `npm run typecheck`, `npm test`, `npm run verify`, `npm run imagegen:qa`, in-app browser super demo with no console errors, and web-game screenshots/states at `output/kof-engine/super-demo.*`, `output/kof-engine/run-forward-demo.*`, and `output/kof-engine/hop-demo.*`.
- Post-final KOF feel pass: fixed repeated same-move hitstop damage, preserved jump/hop state while airborne, allowed airborne/roll pass-through for natural side switching, and refreshed facing after movement/collision. Verification passed: focused combat tests, focused shell/touch/presentation tests, `npm run typecheck`, `npm run verify`, production-preview smoke at `output/web-game/postfinal-kof-feel`, and focused Playwright cross-up proof at `output/web-game/postfinal-kof-crossup`.
- Post-final asset/UI contract guard: exported runtime UI crop specs from Phaser-free config and added tests that pin approved fighter spritesheet dimensions, stage/UI sheet dimensions, and UI crop/canvas bounds. Verification passed: focused asset tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-asset-contract`.
- Post-final visual separation pass: extracted close-range sprite separation into a tested presentation helper, reduced the render-only offset, and disabled it during airborne cross-ups/rolls so visuals align with the new side-switching simulation. Verification passed: focused presentation tests, `npm run verify`, production-preview smoke at `output/web-game/postfinal-visual-separation`, and focused visual cross-up proof at `output/web-game/postfinal-visual-crossup`.
- UI regeneration preflight: live imagegen regeneration is blocked because `OPENAI_API_KEY` is missing, but the bundled imagegen CLI dry-run works with `python3`; all routed runtime UI PNGs were inventoried at `1024x576`. Next safe local slice is to add deterministic UI imagegen jobs so regeneration is ready once credentials are set.
- UI imagegen job prep: added `buildUiImagegenJobs()` for the nine generated Meowtal UI surfaces, preserving stable ids, source paths, prompt slugs, approved statuses, and `1024x576` prompt constraints without importing the prompt-heavy production manifest into the default fighter/stage job path. Verification passed: focused imagegen-job tests and `npm run verify`.
- Dedicated guard gameplay pass: represented grounded guard in deterministic fighter state, made the neutral guard button block incoming attacks without forcing backward movement, preserved walk-back/crouch blocking, and verified touch guard maps to neutral movement plus guard. Verification passed: focused combat/touch tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-guard`.
- KOF mobility pressure pass: prevented stale double-tap history from repeatedly retriggering run/backdash after returning to neutral, and made run/backdash/roll clear dedicated guard so movement and defense cannot overlap. Verification passed: focused combat regressions, full combat tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-pressure`.
- Touch UI polish pass: added canvas-bound/no-overlap/min-size tests for fight controls, re-spaced phone portrait/landscape D-pads so direction zones are unambiguous, enlarged scaled mobile down targets enough for browser smoke, and strengthened procedural button readability/active feedback. Verification passed: focused touch tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-touch-ui`.
- CPU sparring smoothing pass: changed CPU attack reactions to hold dedicated guard in place instead of feeding backward movement into the dash detector, with repeated-pressure coverage to prevent accidental run/backdash oscillation. Verification passed: focused CPU tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-cpu-sparring`.
- Held-guard visual pass: routed idle+guarding snapshots to the approved walk-back defensive row and froze the pose for a stable guard silhouette while preserving deterministic combat state. Verification passed: focused asset-runtime tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-guard-visual`.
- Mobile framing pass: removed the forced portrait canvas top margin so the 16:9 fight window centers naturally on tall phones, and added browser smoke assertions for portrait canvas balance plus landscape vertical fill. Verification passed: `npm run verify` and production-preview smoke at `output/web-game/postfinal-mobile-framing`.
- Production-readiness audit pass: rejected completion because live UI regeneration is still blocked by missing `OPENAI_API_KEY` and roll behavior is the highest-risk unproven KOF-style movement/readability gap. Next executable slice is roll-feel verification and polish without new binary art.
- Roll-feel pass: made light+kick roll take priority over kick attacks, kept active rolls from being interrupted by buffered attack inputs, preserved roll facing through pass-through side switches, and added existing-art motion cues plus browser roll-forward/roll-back proof. Verification passed: focused roll/presentation/animation tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-roll-feel`.
- Endgame UI smoke pass: added production-preview coverage for K.O., delayed round victory, rabbit/cat match victory, win/lose runtime rows, Enter rematch reset, and R reset-to-ready. Verification passed: focused shell/match-flow tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-ko-rematch`.
- Post-roll/endgame readiness audit: no further local gameplay, animation, mobile, or arcade-flow blocker was found after the roll and endgame smoke passes. The remaining unsatisfied owner blocker is credential-backed regeneration of all UI elements; `npm run imagegen:preflight` still reports `OPENAI_API_KEY=missing`, so the next task is ready once the key is set locally.
- UI regeneration credential check: `OPENAI_API_KEY` is still missing, and the UI PNGs in `output/imagegen`, `assets/source/imagegen/ui/meowtal`, and `public/assets/generated/ui/meowtal` are byte-identical 1024x576 files, so there are no unpromoted regenerated UI candidates to approve without live image generation.
- Completion audit checklist: mapped KOF gameplay, animation consistency, UI function, browser proof, production build, small commits, and UI regeneration to concrete evidence; the audit rejects completion only because live UI regeneration remains credential-blocked.
- Runtime UI smoke hardening: fixed the stale missing-UI browser helper so it derives failures from `runtimeUi.assets`, asserted expected visible UI slots across ready/select/fight/roll/K.O./victory/rematch/reset/mobile states, and added CDP-backed right+special multi-touch proof. Verification passed: `npm run verify` and production-preview smoke at `output/web-game/postfinal-ui-visible-slots`.
- Gamepad support pass: added a tested standard Gamepad API mapper, wired connected gamepads into P1 shell controls and combat movement/actions, and added production-preview smoke with a mocked connected gamepad that starts the game, moves P1, and triggers a special. Verification passed: focused gamepad/control/shell tests, `npm run verify`, and smoke at `output/web-game/postfinal-gamepad`.
- Gamepad confirm polish pass: separated South face confirm from Start/Pause, limited South confirm to shell phases, suppressed held South after shell-to-fight transitions until release, and proved confirm-held vs fresh light attack behavior in production-preview smoke. Verification passed: focused gamepad/control/shell tests, `npm run verify`, and smoke at `output/web-game/postfinal-gamepad-confirm`.
- Keyboard confirm polish pass: phase-gated Enter/Space shell confirm so Space remains combat light and Enter remains alternate special during fights, with held-confirm suppression through fight start. Verification passed: focused shell/gamepad/control tests, `npm run verify`, and smoke at `output/web-game/postfinal-keyboard-confirm`.
- Post-input readiness audit: `npm run verify` and production-preview smoke remain green after the confirm-input polish, including desktop keyboard, mocked gamepad, mobile touch, roll, K.O., victory, rematch, and reset coverage at `output/web-game/postfinal-readiness-audit`; completion is still blocked only by `OPENAI_API_KEY=missing` for live UI regeneration.
- Paused touch settings pass: added paused touch CPU toggle and difficulty controls, kept them outside the pause panel across layouts, and proved portrait/landscape paused touch CPU/difficulty behavior in production-preview smoke at `output/web-game/postfinal-paused-touch-settings`. Verification passed: focused touch/shell tests, `npm run verify`, and smoke.
- Frame-pacing pass: added a bounded catch-up guard for the Phaser update loop, exposed frame-pacing telemetry in text state, and added real `requestAnimationFrame` smoke sampling for desktop and mobile scenarios. Verification passed: focused frame-pacing tests, `npm run verify`, and production-preview smoke at `output/web-game/postfinal-frame-pacing`.

## Next Suggestions

- Optional polish beyond this goal: add a complete music theme, add more fighters/stages, add a training/options menu, and package a deploy preview.
